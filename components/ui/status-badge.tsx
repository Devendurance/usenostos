import type { ReactNode } from "react";
export function StatusBadge({ label, tone = "neutral", icon }: { label: string; tone?: "neutral" | "pending" | "available" | "warning"; icon?: ReactNode }) { const toneClass = tone === "pending" ? "border-[var(--lilac)] bg-[#f4f0ff]" : tone === "available" ? "border-[var(--ink)] bg-[var(--blue)]" : tone === "warning" ? "border-[var(--ink)] bg-[var(--sticky-yellow)]" : "border-[var(--line-strong)] bg-white"; return <span className={`inline-flex min-h-8 items-center gap-2 rounded-full border px-3 text-xs font-semibold ${toneClass}`}>{icon ?? <span aria-hidden="true" className="h-1.5 w-1.5 rounded-full bg-current" />}{label}</span>; }
