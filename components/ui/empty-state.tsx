import type { ReactNode } from "react";
export function EmptyState({ title, message, icon, action }: { title: string; message: string; icon?: ReactNode; action?: ReactNode }) { return <div className="flex min-h-52 flex-col items-center justify-center rounded-card border border-dashed border-[var(--line-strong)] bg-[#fbfaf8] p-8 text-center"><div className="mb-4 flex h-11 w-11 items-center justify-center rounded-full border border-[var(--ink)]">{icon ?? "—"}</div><h2 className="display text-xl font-semibold tracking-[-.02em]">{title}</h2><p className="mt-2 max-w-md text-sm leading-6 text-[var(--muted)]">{message}</p>{action && <div className="mt-5">{action}</div>}</div>; }
