import type { ComponentPropsWithoutRef } from "react";
export function Input({ label, hint, error, className = "", id, ...props }: ComponentPropsWithoutRef<"input"> & { label?: string; hint?: string; error?: string }) { const inputId = id ?? props.name; return <div className="flex flex-col gap-2">{label && <label htmlFor={inputId} className="text-sm font-semibold">{label}</label>}<input id={inputId} className={`min-h-12 rounded-control border border-[var(--ink)] bg-white px-4 text-sm outline-none transition-shadow placeholder:text-[var(--muted)] focus-visible:ring-2 focus-visible:ring-[var(--lilac)] ${className}`} aria-invalid={Boolean(error)} aria-describedby={hint || error ? `${inputId}-hint` : undefined} {...props} />{(hint || error) && <p id={`${inputId}-hint`} className={`text-xs leading-5 ${error ? "text-[var(--ink)]" : "text-[var(--muted)]"}`}>{error ?? hint}</p>}</div>; }
