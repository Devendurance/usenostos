import type { ReactNode } from "react";
export function PageHeading({ eyebrow, title, description, actions }: { eyebrow?: string; title: string; description?: string; actions?: ReactNode }) { return <header className="flex flex-col gap-4 border-b border-[var(--line)] pb-8 md:flex-row md:items-end md:justify-between"><div className="max-w-2xl">{eyebrow && <p className="eyebrow mb-3 text-[var(--muted)]">{eyebrow}</p>}<h1 className="display text-balance text-4xl font-bold tracking-[-.04em] md:text-5xl">{title}</h1>{description && <p className="mt-4 max-w-xl text-base leading-7 text-[var(--muted)] text-pretty">{description}</p>}</div>{actions && <div className="shrink-0">{actions}</div>}</header>; }
